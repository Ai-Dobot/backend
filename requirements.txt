# Vital Monitor (local Flask app)
flask

# Render FastAPI backend (backend.py) — same stack as doctor-app
fastapi
uvicorn[standard]
requests
google-auth
google-auth-httplib2
httplib2
psycopg2-binary
python-multipart
cloudinary
